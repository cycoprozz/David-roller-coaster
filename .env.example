# Optional: only use public/browser-safe values here.
# Do NOT put private API keys in VITE_* variables.
# Basic state/region average price endpoint:
# VITE_GAS_PRICE_API_URL="https://your-gas-api.example.com/prices?region={region}"
